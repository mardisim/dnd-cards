# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build dnd-cards`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t dnd-cards`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=4200

WORKDIR /app

RUN addgroup --system dnd-cards && \
    adduser --system -G dnd-cards dnd-cards

COPY dist/apps/dnd-cards dnd-cards/
RUN chown -R dnd-cards:dnd-cards .

CMD [ "node", "dnd-cards/server/main.js" ]
